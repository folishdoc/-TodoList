# ============================================================
# 开发环境配置 — 使用 `--spring.profiles.active=dev` 激活
# ============================================================

# H2 Console（开发调试用）
spring.h2.console.enabled=true

# JPA SQL 日志
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
